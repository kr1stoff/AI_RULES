# CODING

## 准则

1. `Python` 代码遵循 `PEP-8` 规范, 不要写抽象代码
2. 不要设计抽象复杂的代码单元, 代码简单直接
3. 不要造无用的轮子, 效率优先

## 风格

1. 除基础元素赋值或简单逻辑外，逐行写注释
2. 类和函数输入输出参数要标明类型

## 习惯

1. 每次执行完分析都总结后追加更新到工作目录下的 `AGENTS.md` 文件, 如果不存在则新建
2. `Python` 表格处理优先用 `pandas`, 作图用 `seaborne` 和 `matplotlib`, 无法实现或提示词有明确指定库情况除外